/** @type {import('next').NextConfig} */
    const nextConfig = {
      images: {
        domains: ['fonts.googleapis.com', 'www.googleapis.com', 'www.youtube.com', 'www.w3.org'],
      },
    };
    
    export default nextConfig;